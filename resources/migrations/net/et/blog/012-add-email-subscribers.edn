{:id "012-add-email-subscribers"
 :up ["CREATE TABLE IF NOT EXISTS email_subscribers (
         email TEXT PRIMARY KEY,
         created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')))"]
 :down ["DROP TABLE IF EXISTS email_subscribers"]
 :transactions false}
